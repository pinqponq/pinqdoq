---
paths: ['__claude-md-not-a-rule__/**']
---

@common.md

# pinq-doq

This repo contains shared Claude rules for PinqPonq projects.
When editing rule files, maintain the existing structure and heading hierarchy.
Do not add rules that are already covered in another file — check for duplication first.
When adding or editing rule files, follow the `paths:` loading convention in `README.md` ("How rules load"): scope stack-specific rules, and keep reference docs / non-rules out of auto-load.
